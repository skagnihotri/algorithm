#include <bits/stdc++.h>
using namespace std;

int main()
{
	int n;
	cin>>n;
	int arr[n];
	for (int i = 0; i < n; ++i){
		cin>>arr[i];
	}

	unordered_map<int, int> mp;
	vector<int> vec;
	for (int i = 0; i < ; ++i){
		mp[arr[i]] = i;
	}
	
	return 0;
}